package usecase.logout;

/**
 * Input boundary interface for logging out.
 * Defines the method to initiate the logout process.
 */
public interface LogoutInputBoundary {
    /**
     * Initiates the logout process.
     */
    void logout();
}
